# Non-Normative Anchor Fixture

## Critical Invariants

- This section intentionally omits the required invariant.

## Example

```markdown
- Same target agent, same task, same context, same constraints, and same output instructions for every run; only the requested model route may differ.
```

This fixture should fail because the anchor appears only in a non-normative example.