# Build and Runtime

> Generated by Code Explorer on 2026-06-13 at commit `fixture`. Scope: fixtures/tiny-node-api. Mode: initial.

## How to install dependencies

None required; the fixture uses only Node.js built-ins.

## How to build

No build step.

## How to run

`node src/server.js`

## How to test

`npm test` (runs `node --test`).

## Runtime services

A single HTTP server (`src/server.js`).

## External dependencies

None.

## Environment variables

| Variable | Purpose | Required? | Default | Evidence |
|---|---|---:|---|---|
| GREETING_PREFIX | Prefix for greetings | No | `Hello` | `src/config.js` |
| PORT | HTTP listen port | No | `3000` | `src/config.js` |

## CI/CD workflows

None present.

## Observed command results

Not run (fixture).

## Limitations

- No deployment or CI configuration exists in the fixture.
