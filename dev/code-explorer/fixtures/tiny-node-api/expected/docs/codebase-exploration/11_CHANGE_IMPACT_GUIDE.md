# Change Impact Guide

> Generated by Code Explorer on 2026-06-13 at commit `fixture`. Scope: fixtures/tiny-node-api. Mode: initial.

## Area: greeting endpoint

### What it does

Serves `GET /greeting` and reflects a `name` parameter into a greeting string.

### Files likely involved

- `src/routes.js`, `src/service.js`, `src/config.js`

### Downstream dependencies

- `buildGreeting` consumers; the response contract is plain text.

### Contracts to preserve

- `GET /greeting?name=...` returns `text/plain`.

### Tests to run

- `npm test` (currently only covers the service).

### Risks when changing

- Changing the reflection behavior interacts with RISK-001.

### Safe change strategy

- Add an input bound and a route-level test before altering output formatting.

### Observability / rollout checks

- None configured in the fixture.

## Limitations

- Only one area exists.
