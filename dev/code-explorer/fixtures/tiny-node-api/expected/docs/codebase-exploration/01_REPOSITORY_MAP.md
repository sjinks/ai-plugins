# Repository Map

> Generated by Code Explorer on 2026-06-13 at commit `fixture`. Scope: fixtures/tiny-node-api. Mode: initial.

## Languages

- JavaScript (ES modules) — High — `package.json` has `"type": "module"`; all sources are `.js`.

## Frameworks

- None; uses the Node.js built-in `http` module directly.

## Package managers

- npm — `package.json` present.

## Build/test tooling

- Node.js built-in test runner (`node --test`).

## Top-level directories

| Path | Purpose | Confidence | Evidence |
|---|---|---:|---|
| src/ | Application source | High | `src/server.js` starts the HTTP server |
| test/ | Tests | High | `test/service.test.js` uses `node:test` |

## Generated/vendor/build-output paths to ignore

- `node_modules/` (not present in the fixture).

## Notable configuration files

- `package.json` — package metadata and the `test` script.

## Limitations

- No CI, Docker, or deployment configuration exists in the fixture.
