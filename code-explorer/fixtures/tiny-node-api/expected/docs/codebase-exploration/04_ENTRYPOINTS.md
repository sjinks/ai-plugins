# Entrypoints

> Generated by Code Explorer on 2026-06-13 at commit `fixture`. Scope: fixtures/tiny-node-api. Mode: initial.

## Summary

| Entrypoint | Type | File | Auth | Side effects | Risk | Confidence |
|---|---|---|---|---|---|---:|
| GET /greeting | http | `src/routes.js` | none | none | Medium | High |

## Entrypoint: GET /greeting

### Location

`src/routes.js`: `handleGreeting()`

### Trigger

HTTP `GET /greeting?name=...`.

### Inputs

- `name` query parameter (untrusted).

### Validation

None found. The `name` value is passed to `buildGreeting` unmodified.

### Authentication / authorization

None.

### Call chain

```text
HTTP GET /greeting
  -> routes.js route()
  -> routes.js handleGreeting()
  -> service.js buildGreeting()
  -> config.js loadConfig()
```

### Side effects

None beyond the HTTP response.

### Errors

Unknown paths return 404; no error handling around `buildGreeting`.

### Tests

`test/service.test.js` covers `buildGreeting` but not the HTTP layer.

### Risks

- RISK-001: reflected `name` without length limit or sanitization.

### Open questions

- QUESTION-001: is there an intended maximum length for `name`?

## Limitations

- The HTTP handler itself has no direct test.
